var expect = require("chai").expect;

describe("JSSQL TEST", function () {
    describe("Testing the test", function () {
        it("should return true back true", function (done) {
            expect(true).to.equal(true);

            done();
        });
    })
});